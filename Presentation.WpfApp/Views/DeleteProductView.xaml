<UserControl x:Class="Presentation.WpfApp.Views.DeleteProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:DeleteProductViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Delete a product" FontSize="25" FontWeight="Bold" Foreground="AliceBlue" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="ID" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductID" Text="{Binding ProductId.Id}" />
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Button Padding="8" Content="Delete product" x:Name="DeleteProduct" Command="{Binding DeleteProductCommand}"/>
            <Button Padding="8" Content="Go to main menu" Command="{Binding GoToMainMenuCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
