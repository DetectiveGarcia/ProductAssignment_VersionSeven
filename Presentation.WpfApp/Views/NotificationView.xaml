<UserControl x:Class="Presentation.WpfApp.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:NotificationViewModel}"
             mc:Ignorable="d" 
             >

    <Border
        Width="250"
        Height="250"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        >
        <Grid Background="#1a1a1a">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Notfication" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Foreground="{Binding Color, FallbackValue=AliceBlue}" />

            <TextBlock Grid.Row="1" Text="{Binding Notification, FallbackValue=Hello}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="AliceBlue" FontSize="15" FontWeight="Bold"/>
                <Button Grid.Row="2" Content="Ok" Margin="60 20" Command="{Binding OkBtnCommand}" />
        </Grid>
    </Border>
</UserControl>
