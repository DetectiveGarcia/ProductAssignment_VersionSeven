<UserControl x:Class="Presentation.WpfApp.Views.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:AddProductViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Create a product" FontSize="25" FontWeight="Bold" Foreground="AliceBlue" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Name" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductName" Text="{Binding ProductRequest.Name}" />
        </StackPanel>
        <StackPanel Grid.Row="2">
            <TextBlock Text="Description (optional)" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductDescription" Text="{Binding ProductRequest.Description}"  />
        </StackPanel>
        <StackPanel Grid.Row="3">
            <TextBlock Text="Price" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductPrice" Text="{Binding ProductRequest.Price}" />
        </StackPanel>
        <StackPanel Grid.Row="4">
            <TextBlock Text="Category" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductCategory" Text="{Binding ProductRequest.Category.Name}" />
        </StackPanel>
        <StackPanel Grid.Row="5">
            <TextBlock Text="Manufacture" Foreground="AliceBlue"/>
            <TextBox x:Name="TextProductManufacture" Text="{Binding ProductRequest.Manufacture.Name}" />
        </StackPanel>
        <StackPanel Grid.Row="6">
            <Button Padding="8" Content="Add product" x:Name="AddProduct" Command="{Binding AddProductCommand}"/>
            <Button Padding="8" Content="Go to main menu" Command="{Binding GoToMainMenuCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
