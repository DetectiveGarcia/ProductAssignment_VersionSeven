<UserControl x:Class="Presentation.WpfApp.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainMenuViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Main Menu" FontSize="25" FontWeight="Bold" Foreground="AliceBlue" HorizontalAlignment="Center" VerticalAlignment="Center"  />
        <StackPanel Grid.Row="1">
            <Button Padding="8" Content="Create product" Command="{Binding GoToAddProductCommand}" />
        </StackPanel>
        <StackPanel Grid.Row="2" >
            <Button Padding="8" Content="Update product" Command="{Binding GoToUpdateProductCommand}" />
        </StackPanel>
        <StackPanel Grid.Row="3">
            <Button Padding="8" Content="Delete product" Command="{Binding GoToDeleteProductCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="4">
            <Button Padding="8" Content="Product list" Command="{Binding GoToProductListCommand}" />
        </StackPanel>
        <StackPanel Grid.Row="5">
            <Button Padding="8" Content="Exit" Command="{Binding ExitCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
